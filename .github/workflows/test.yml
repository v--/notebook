name: Test

on:
  push:
    branches:
      - 'master'

jobs:
  test:
    runs-on:  ubuntu-slim

    steps:
    - uses: actions/checkout@v7

    - name: Setup uv
      uses: astral-sh/setup-uv@v9.0.0

    - name: Install dependencies
      run: uv sync --frozen

    - name: Lint
      run: |
        uv run --no-sync ruff check
        uv run --no-sync poe find-blank-first-line

    - name: Typing
      run: uv run --no-sync mypy

    - name: Test
      run: uv run --no-sync pytest
