\section{Categories}\label{sec:categories}

\paragraph{Categories}

\begin{definition}\label{def:category}\mcite[7-10]{MacLane1998CategoryTheory}
  A \term{category} \( \cat{C} \) is a \hyperref[def:directed_multigraph]{directed multigraph} with additional structure:

  \begin{thmenum}[series=def:category]
    \thmitem{def:category/objects} We call the vertices of the graph \term[ru=объекты (\cite[def. I.1.1]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{objects} and denote the set of all objects by \( \obj(\cat{C}) \).

    As a shorthand for \( A \in \obj(\cat{C}) \), we also write \( A \in \cat{C} \).

    \thmitem{def:category/morphisms} We call the arcs of the graph \term[ru=морфизм (\cite[def. I.1.1]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий}), en=morphism (\cite[def. 1.1.1]{Perrone2024StartingCategoryTheory})]{morphisms} or simply \term[en=arrows (\cite[def. 1.1.1]{Perrone2024StartingCategoryTheory})]{arrows}.

    For every morphism \( f \) from \( A \) to \( B \), we use the notation \( f: A \to B \) or \( A \reloset f \to B \). We call the initial vertex \( A \) the \term{domain} of \( f \) and denote it by \( \dom(f) \), and dually, we call its terminal vertex \( B \) its \term{codomain} and denote it by \( \co\dom(f) \).

    Additionally, we denote the set of all morphisms from \( A \) to \( B \) by \( \cat{C}(A, B) \) or, in a more algebraic tradition, by \( \hom_{\cat{C}}(A, B) \). Naturally, we call \( \hom(A, B) \) a \term{\( \hom \)-set}.

    Both notations highlight that \( \hom(A, B) \), when parameterized, becomes \hyperref[def:functor]{functor} --- see \cref{def:hom_functor}.

    \thmitem{def:category/composition} We generalize \hyperref[def:set_valued_map/composition]{function composition} to a \hyperref[def:set_valued_map/partial]{partial operation} on morphisms, also called \term[ru=композиция (\cite[41]{Хелемский2014ФункциональныйАнализ})]{composition} and denoted by \( {\bincirc} \).

    We allow composing \( g: B \to C \) with \( f: A \to B \) to obtain \( (g \bincirc f): A \to C \).

    The order in which we write the operands is based on function composition rather than multiplication in \hyperref[def:group]{(semi)groups}; this is discussed in \cref{rem:category_composition_order}.

    \thmitem{def:category/identity} Finally, for every object \( A \), we pick a special morphism from \( A \) to itself. We call it the \term{identity} on \( A \) and denote it by \( \id_A \).
  \end{thmenum}

  The definition of a category additionally requires the following conditions to hold:
  \begin{thmenum}[resume=def:category]
    \thmitem[def:category/C1]{C1} For any morphism \( f: A \to B \), the identities \( \id_A \) and \( \id_B \) must acts like \hyperref[def:semigroup_neutral_element]{neutral elements} and satisfy
    \begin{equation}\label{eq:def:category/C1}\tag{\logic{C1}}
      f \bincirc \id_A = \id_B \bincirc f = f.
    \end{equation}

    \thmitem[def:category/C2]{C2} Composition must be \hyperref[def:binary_operation/associative]{associative} --- for each compatible triple of morphism \( f: A \to B \), \( g: B \to C \) and \( h: C \to D \), the following must hold:
    \begin{equation}\label{eq:def:category/C2}\tag{\logic{C2}}
      (h \bincirc g) \bincirc f = h \bincirc (g \bincirc f).
    \end{equation}
  \end{thmenum}
\end{definition}
\begin{comments}
  \item Any set-theoretic encoding of a category \( \cat{C} \) must contain the set \( \obj(\cat{C}) \) of objects and, for every pair \( A \) and \( B \) of objects, the set \( \hom(A, B) \) of morphisms between them. The particular encoding is of secondary importance. For definiteness, we can work with \hyperref[def:hol_semantics/model]{models} of the \hyperref[def:many_sorted_fol]{two-sorted first-order} theory presented in \cref{rem:hol_theory_of_categories}.

  \item Categories are unrelated to \hyperref[def:categorical_logical_theory]{logical theory categoricity} --- see \cref{rem:categorical_theory_terminology} --- nor to \hyperref[def:meager_set]{Baire categories} --- see \cref{rem:baire_category_terminology}.

  \item Categories are not commonly defined via graphs, but such a definition is suggested by Saunders Mac Lane, one of the creators of the category theory. This is discussed in \cref{rem:category_as_graph}.

  \item Essential categories require extensions to the underlying set theory, even though the definition of category does not. See \cref{rem:category_size}.
\end{comments}

\begin{remark}\label{rem:category_composition_order}
  In a \hyperref[def:category]{category}, the order of the composition generalizes \hyperref[def:set_valued_map/composition]{function composition} rather than multiplication in \hyperref[def:group]{(semi)groups}.

  \begin{thmenum}
    \thmitem{rem:category_composition_order/contravariant} Namely, the composition of \( f: A \to B \) and \( g: B \to C \) is \( g \bincirc f \) in
    \cite[7]{MacLane1998CategoryTheory}
    \cite[def. 1.1.1]{Schubert1972Categories}
    \cite[def. 1.1.1]{Riehl2016CategoryTheory},
    \cite[def. 1.1.1]{Perrone2024StartingCategoryTheory},
    \cite[def. 1.1.1]{Leinster2014BasicCategoryTheory},

    This also includes some algebra books like
    \cite[def. I.3.1]{Aluffi2009Algebra},
    \cite[53]{Lang2002Algebra},
    \cite[def. 1.1]{Jacobson1989BasicAlgebraII},
    \cite[268]{Шафаревич2019ОсновныеПонятияАлгебры}.

    \thmitem{rem:category_composition_order/covariant} The order \( f \bincirc g \) is instead used in
    \cite[def. I.1.1]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий}.
  \end{thmenum}
\end{remark}

\begin{example}\label{ex:def:category}
  Examples of categories include:

  \begin{itemize}
    \item The category \( \cat{Set} \) of \hyperref[def:small_set]{small} \hyperref[def:set]{sets} and \hyperref[def:function]{functions} defined in \cref{def:category_of_small_sets}.

    \item The category \( \cat{Cat} \) of small categories defined in \cref{def:category_of_small_categories}.

    \item All the \hyperref[def:fol_theory_model_functor/obj]{categories of small first-order models}. A list can be found in \cref{ex:def:fol_theory_model_functor}

    \item The category \( \cat{Top} \) of small \hyperref[def:topological_space]{topological spaces} and \hyperref[def:global_continuity]{continuous functions} defined in \cref{def:category_of_small_topological_spaces}.

    \item For every topological space, the fundamental groupoid defined in \cref{def:fundamental_groupoid}.

    \item The category of small \hyperref[def:directed_multigraph]{directed multigraphs} defined in \cref{def:directed_multigraph/category}, as well as related categories discussed in \cref{def:graph_functors}.

    \item For every directed multigraph, the free category defined in \cref{def:directed_multigraph_free_category}.

    \item For every \hyperref[def:preordered_set]{preordered set}, the induced category defined in \fullref{thm:order_category_isomorphism}.
  \end{itemize}
\end{example}

\begin{remark}\label{rem:category_as_graph}
  \Textcite[37]{Johnstone1982StoneSpaces} attributes the concept of a category to the papers \cite{EilenbergMacLane1942NaturalIsomorphisms} and \cite{EilenbergMacLane1945NaturalEquivalences}, both by Samuel Eilenberg and Saunders Mac Lane. The earlier one only discusses \hyperref[def:functor]{functors}, so the latter can be considered \enquote{the} first definition of a category. It presents a category as a pair of \enquote{classes} (without clarifying what a \enquote{class} means) --- one for objects and one for morphisms --- with the rest of the definition laid out explicitly.

  Several decades later, in his quintessential reference for categories, \textcite[ch. I]{MacLane1998CategoryTheory} states a more refined definition. In an informal \hyperref[con:metalanguage]{metalanguage}, he lists axioms for categories in what amounts to be \hyperref[def:many_sorted_fol]{two-sorted first-order logic}. We present a proper formulation in \cref{rem:hol_theory_of_categories}.

  Mac Lane calls any \hyperref[con:logical_model]{model} of these axioms a \enquote{metacategory}, assuming that such a model can be abstract and not necessarily based on sets\fnote{In a modern formulation, he allows a more general \hyperref[def:institution]{institution} than that from \cref{def:hol_institution}.}. In fact, he bases the notion on \enquote{metagraphs} --- abstract models of directed multigraphs. He calls the set-based models \enquote{categories} and \enquote{graphs}, correspondingly.

  Technically, this allows some flexibility in choosing an underlying set theory; this is not a straightforward choice, as we discuss in \cref{rem:category_size}.

  Many authors prefer the earlier approach of taking a class of objects and a class of morphisms and imposing properties on them. This includes
  \cite[def. 1.1.1]{Schubert1972Categories}
  \cite[def. 1.1.1]{Riehl2016CategoryTheory},
  \cite[def. 1.1.1]{Perrone2024StartingCategoryTheory},
  \cite[def. 1.1.1]{Leinster2014BasicCategoryTheory},
  \cite[def. I.1.1]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий},
  as well as some algebra books like
  \cite[def. I.3.1]{Aluffi2009Algebra},
  \cite[53]{Lang2002Algebra},
  \cite[def. 1.1]{Jacobson1989BasicAlgebraII},
  \cite[268]{Шафаревич2019ОсновныеПонятияАлгебры}.
\end{remark}

\begin{remark}\label{rem:hol_theory_of_categories}
  Categories, as defined in, \cref{def:category} can be described via \hyperref[def:many_sorted_fol]{two-sorted first-order logic}.

  We start with the two-sorted \hyperref[def:hol_signature]{signature} for \hyperref[def:directed_multigraph]{directed multigraphs} from \cref{rem:logical_theory_of_graphs/directed_multigraphs}, where \( \syn\kappa \) is the sort of vertices (objects) and \( \syn\alpha \) is the sort of arcs (morphisms).

  In addition to the \hyperref[def:hol_signature/nl_const]{nonlogical constants}
  \begin{align*}
    \synH&: \syn\alpha \synimplies \syn\kappa, \\
    \synT&: \syn\alpha \synimplies \syn\kappa,
  \end{align*}
  determining the endpoints of a morphism, we must also add
  \begin{align*}
    \synmult&: \syn\alpha \synimplies \syn\alpha \synimplies \syn\alpha, \\
    \syn\id&:  \syn\kappa \synimplies \syn\alpha,
  \end{align*}
  subject to the axioms
  \begin{align*}
    &\qforall {\synf^{\syn\kappa}} \qforall {\syng^{\syn\kappa}} \parens[\big]{ \synT(\synf) \syneq \synH(\syng) \synimplies (\synH(\synf) \syneq \synH(\syng \synmult \synf) \synwedge \synT(\syng) \syneq \synT(\syng \synmult \synf)) }, \\
    &\qforall {\synf^{\syn\kappa}} \qforall {\syng^{\syn\kappa}} \qforall {\synh^{\syn\kappa}} \parens[\big]{ (\synh \synmult \syng) \synmult \synf \syneq \synh \synmult (\syng \synmult \synf) }
  \end{align*}
  for composition and
  \begin{balign*}
    &\qforall {\syna^{\syn\kappa}} \parens[\big]{ \synT(\syn\id(\syna)) \syneq \syna }, \\
    &\qforall {\syna^{\syn\kappa}} \parens[\big]{ \synH(\syn\id(\syna)) \syneq \syna }, \\
    &\qforall {\synf^{\syn\alpha}} \parens[\big]{ \synf \synmult \syn\id(\synH(\synf)) \syneq \synf }, \\
    &\qforall {\synf^{\syn\alpha}} \parens[\big]{ \syn\id(\synT(\synf)) \synmult \synf \syneq \synf }
  \end{balign*}
  for identity morphisms.

  Note that, as per \cref{rem:hol_definite_description/interpretation}, our formulation of higher-order logic does not allow partially-defined operations. Thus, composition must be total, and incompatible morphisms must be composable. For definiteness, we may add the following axiom:
  \begin{equation*}
    \qforall {\synf^{\syn\kappa}} \qforall {\syng^{\syn\kappa}} \parens[\big]{ \synneg (\synT(\synf) \syneq \synH(\syng)) \synimplies (\syng \synmult \synf \syneq \syn\id(\synH(\synf))) }.
  \end{equation*}

  Fixing such an \enquote{error value} helps avoid the case where multiple categories differ only by how they treat ill-defined composition.
\end{remark}

\paragraph{Set and category size}

\begin{definition}\label{def:small_set}\mcite[17]{Shulman2008SetTheoryForCategoryTheory}
  We are faced with a conundrum --- we want to study the category of all sets, but set theory cannot express such a construct. Our solution is to work in \hyperref[def:zfcu]{\logic{ZFC+U}}, where in addition to the standard axioms of set theory, we presuppose the existence of at least one \hyperref[def:grothendieck_universe]{Grothendieck universe}, and hence of the \hyperref[def:smallest_grothendieck_universe]{smallest Grothendieck universe} \( \mscrU_0 \). As shown in \cref{thm:grothendieck_universe_is_model_of_zfc}, \( \mscrU \) is itself a \hyperref[def:fol_semantics/model]{model} of \hyperref[def:zfc]{\logic{ZFC}}, so studying only members of \( \mscrU \) is not inherently limited compared to studying arbitrary sets in \logic{ZFC}.

  We refer to the members of \( \mscrU_0 \) as \term{small sets}. This terminology applies more generally to other structures. Groups, lattices or even categories may just as well be small if their set-theoretic encoding is a small set. This is formalized in \cref{thm:hol_small_model_characterization}, \cref{thm:fol_small_model_characterization} and \cref{thm:small_category_characterization}.

  If we want to highlight that a set or other structure is not small, we say that it is \term{large}.
\end{definition}
\begin{comments}
  \item Alternative approaches for discriminating small and large sets are described in \cref{rem:category_size}.
\end{comments}

\begin{proposition}\label{thm:hol_small_model_characterization}
  A \hyperref[def:hol_structure]{structure of higher-order logic} \( \mscrX = (X, I) \) over the \hyperref[def:hol_frame]{frame} \( X = \seq{ X_\tau }_{\tau \in \op*{\BbbQ_\Sigma}} \) is itself a \hyperref[def:small_set]{small set} if and only if, for every sort \( S \) in \( \Sigma \), the set \( X_\tau \) is small.
\end{proposition}
\begin{proof}
  \SufficiencySubProof Suppose that, as an \hyperref[def:ordered_tuple]{ordered pair}, \( \mscrX = (X, I) \) is small.

  We have not specified whether we treat \( \mscrU \) as a \hyperref[def:kuratowski_pair]{Kuratowski pair} or as an \hyperref[def:indexed_family]{indexed family}, but both lead to the same result.
  \begin{itemize}
    \item In the first case, we have
    \begin{equation*}
      \mscrX = \set{ \set{ X }, \set{ X, I } }.
    \end{equation*}

    By the transitivity axiom \ref{def:grothendieck_universe/transitive}, the set \( \set{ X, I } \) is small, and hence so are \( X \) and \( I \).

    \item In the second case, we have
    \begin{equation*}
      \mscrX = \set{ \braket{ 0, X }, \braket{ 1, I } },
    \end{equation*}
    where both \( \braket{ 0, X } \) and \( \braket{ 1, I } \) are Kuratowski pairs. By the above reasoning, both \( X \) and \( I \) are again small.
  \end{itemize}

  Continuing analogously, since \( X \) is an indexed family, we know that, for each quantifiable type \( \tau \), the pair \( \braket{ \tau, X_\tau } = \set{ \set{ \tau }, \set{ \tau, X_\tau } } \) belongs to \( X \), and are hence small. Then, for every type \( \tau \), the set \( X_\tau \) is small; in particular, \( X_S \) is small for every sort \( S \).

  \NecessitySubProof Suppose that \( X_S \) is small for every sort \( S \).

  We use \fullref{thm:induction_on_abstract_syntax} on the quantifiable type \( \tau \) to show that \( X_\tau \) is small.
  \begin{itemize}
    \item If \( \tau \) is a sort, this holds by assumption.

    \item Suppose that \( \tau \) is the type of propositions \( \syn\omicron \).

    In the set \( X_{\syn\omicron} = \set{ \semtop, \sembot } \), both \( \semtop \) and \( \sembot \) are small by the assumption from \cref{rem:language_alphabet_cardinality}, and \cref{thm:grothendieck_universe_closure/unordered_pair} implies that \( X_{\syn\omicron} = \set{ \semtop, \sembot } \) also is small.

    \item If \( \tau = \sigma \synimplies \rho \), where the inductive hypothesis holds for \( \sigma \) and \( \rho \), then
    \begin{equation*}
      X_{\tau \synimplies \sigma} \subseteq \fun(X_\tau, X_\sigma) \subseteq X_\tau \times X_\sigma.
    \end{equation*}

    By the inductive hypothesis, both \( X_\tau \) and \( X_\sigma \) belong are small. \Cref{thm:grothendieck_universe_closure/cartesian_product} implies that their product \( X_\tau \times X_\sigma \) also is small.

    Any subset of the product, in particular \( X_{\tau \synimplies \sigma} \), also is small due to \cref{thm:grothendieck_universe_closure/subset}.
  \end{itemize}

  Thus, we have concluded that \( X_\tau \) is small for every quantifiable type \( \tau \). By \cref{thm:grothendieck_universe_closure/indexed_family}, the entire frame \( X = \seq{ X_\tau }_{\tau \in \op*{\BbbQ_\Sigma}} \) also is small.

  The interpretation \( I \) takes some constant \( c: \tau \) to an element of \( X_\tau \). The signature is assumed to be small, while \( X_\tau \) is, as we have just shown, is also small. \Cref{thm:grothendieck_universe_closure/function} implies that the function \( I \), as a set, also is small.

  Finally, \cref{thm:grothendieck_universe_closure/indexed_family} implies that the ordered pair \( \mscrX = (X, I) \) is small.
\end{proof}

\begin{proposition}\label{thm:fol_small_model_characterization}
  A \hyperref[def:fol_structure]{structure of first-order logic} \( \mscrX = (X, I) \) is itself a \hyperref[def:small_set]{small set} if and only if \( X \) is small.
\end{proposition}
\begin{proof}
  Similar to, but simpler than \cref{thm:hol_small_model_characterization}.
\end{proof}

\begin{proposition}\label{thm:small_category_characterization}
  A \hyperref[def:category]{category} is itself a \hyperref[def:small_set]{small set} if and only if the set of all its morphisms is small.
\end{proposition}
\begin{proof}
  Let \( \mscrX = (X, I) \) be a \hyperref[def:hol_structure]{structure of higher-order logic} that satisfies the theory of categories presented in \cref{rem:hol_theory_of_categories}.

  \Cref{thm:hol_small_model_characterization} implies that \( \mscrX \) is small if and only if \( X_{\syn\kappa} \), the set of objects, and \( X_{\syn\alpha} \), the set of morphisms, are small.

  But \( X_{\syn\alpha} \) contains a distinct identity morphism for each object in \( X_{\syn\kappa} \), so if \( X_{\syn\alpha} \) if small, so is \( X_{\syn\kappa} \).

  Therefore, \( \mscrX \) is a small set if and only if \( X_{\syn\alpha} \) is.
\end{proof}

\begin{definition}\label{def:small_category}\mcite[3; 10]{Shulman2008SetTheoryForCategoryTheory}
  We call a \hyperref[def:category]{category} \term[ru=малая категория (\cite[15]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{small} if the set of all its morphisms is \hyperref[def:small_set]{small}. \Cref{thm:small_category_characterization} shows that this is equivalent to the category itself being a small set.

  If the individual \( \hom \)-sets are small, but not necessarily the set of all morphisms, we call the category \term[ru=локально малая категория (\cite[26]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{locally small}.

  As in \cref{def:small_set}, if we want to highlight that a category is not small, we say that it is \term{large}.
\end{definition}
\begin{comments}
  \item Alternative approaches for discriminating small and large categories are described in \cref{rem:category_size}.
\end{comments}

\begin{remark}\label{rem:category_size}
  In \cref{def:small_set}, we have introduced a distinction between \enquote{small sets}, those that belong to the \hyperref[def:smallest_grothendieck_universe]{smallest Grothendieck universe}, and \enquote{large sets}, those that do not. This also allowed us to distinguish small from large categories in \cref{def:small_category}.

  In this, we went beyond the conventional \hyperref[def:zfc]{Zermelo-Fraenkel set theory} by assuming the existence of a \hyperref[def:grothendieck_universe]{Grothendieck universe}. \Cref{thm:grothendieck_universe_iff_regular_strong_limit} shows that this assumption is equivalent to the existence of a \hyperref[rem:strongly_inaccessible_cardinal]{strongly inaccessible cardinal}.

  Fix a standard model \( \mscrV = (V, I) \) of \logic{ZFC}. Our assumption amounts to the existence if \( V \) of some universe \( \mscrU \). By taking the intersection of all such universes, we arrive at the smallest one, which we denote by \( \mscrU_0 \).

  This is essentially the approach used by \textcite[12]{MacLane1971CategoricalAlgebraFoundations}. We list some other approaches for comparison.

  \begin{thmenum}
    \thmitem{rem:category_size/grothendieck} We can go further and assume that each set \( A \) belongs to some universe \( \mscrU \). In particular, \( A \) can itself be universe. We call this the \term{axiom of universes}. \Textcite[234]{MacLane1971CategoricalAlgebraFoundations} attributes this approach to \enquote{Grothendieck and his followers}.

    This gives us a sequence of nested universes, somewhat alike the sequence of type universes from \cref{con:type_universe}.

    However, instead of a single category \( \cat{Set} \) of all small sets, we now have a category \( \cat[\mscrU]{Set} \) of \( \mscrU \)-small sets for every universe \( \mscrU \). An obvious drawback is what \textcite[22]{Shulman2008SetTheoryForCategoryTheory} calls \enquote{universe-juggling}. We may either by default assume a fixed universe, which has little advantage of working with \( \mscrU_0 \), or make the universes explicit, which is cumbersome.

    \Textcite[\S 3.3]{Schubert1972Categories} uses this approach, but works in one fixed universe. Thus, even though Schubert assumes the existence of infinitely many universes, the resulting theory is not very different from ours.

    \thmitem{rem:category_size/classes} An alternative is to work in an extension of \( \logic{ZFC} \) that supports classes, such as \hyperref[def:godel_bernays_set_theory]{von Neumann-G\"odel-Bernays set theory} (\logic{NBG}). This approach is used by \textcite[def. I.1.1]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий}.

    Here, we can start with an ordinary standard model \( \mscrV = (V, I) \) of \logic{ZFC} and build a model \( \mscrV' = (\pow(V), I') \) of \logic{NBG}, where the members of \( V \) are considered sets and the subsets of \( V \), i.e. the members of \( \pow(V) \), are considered proper classes.

    Given a class \( O \) of objects and a class \( M \) of morphisms, \cref{thm:ordered_tuple_via_disjoint_union} suggests that the set \( (\set{ 0 } \times O) \cup (\set{ 1 } \times M)) \) can be used to emulate an \hyperref[def:ordered_tuple]{ordered tuple}, and thus to formally encode a category as a class.

    We can form the category \( \cat{Set} \) of all sets and the category \( \cat{Cat} \) of small categories, i.e. those that are sets. Both categories are \enquote{large} and belong to the even larger category \( \cat{CAT} \) of large categories.

    This approach requires us to be very careful with our formalisms because important constructs like the \hyperref[def:functor_category]{functor category} can only be properly defined for small categories.

    \thmitem{rem:category_size/reflection} Yet another approach is to construct a \hyperref[def:fol_theory/conservative]{conservative} extension of \logic{ZFC} by introducing a \hyperref[def:fol_signature/constants]{constant symbol} \( \syn\BbbS \) with some axioms that make it usable as a universe. \Textcite[25]{Shulman2008SetTheoryForCategoryTheory} calls this theory \logic{ZFC/S} and attributes it to Solomon Feferman.

    First, this pseudo-universe \( \syn\BbbS \) must satisfy some basic coherence axioms
    \begin{align*}
      \qforall {\synx \synin \syn\BbbS} \qforall {\syny \synin \synx} \syny \synin \syn\BbbS,
      \qforall {\synx \synin \syn\BbbS} \qforall {\syny \synsubseteq \synx} \syny \synin \syn\BbbS.
    \end{align*}

    More importantly, \( \syn\BbbS \) must satisfy the \term{reflection principle} --- for every formula \( \varphi \) in plain \logic{ZFC} with free variables \( x_1, \ldots, x_n \), we need the axiom
    \begin{equation*}
      \qforall* {x_1 \in \syn\BbbS} \ldots \qforall* {x_n \in \syn\BbbS} (\varphi \syniff \varphi^\syn\BbbS),
    \end{equation*}
    where \( \varphi^\syn\BbbS \) \hyperref[rem:fol_formula_conventions/relativization]{relativizes} every quantifier in \( \varphi \) to \( \syn\BbbS \). Thus, if a (closed) sentence is proved for all members of \( \syn\BbbS \), this proof should transfer to all sets in \logic{ZFC}.

    Although \( \syn\BbbS \) is usable as a universe, it has a major drawback --- a function between members of \( \syn\BbbS \) does not necessarily belong to \( \syn\BbbS \). This breaks many concepts such as \hyperref[def:functor_category]{functor categories}.

    \Textcite[26]{Shulman2008SetTheoryForCategoryTheory} claims that if a form of the \hyperref[def:zfc/replacement]{axiom of replacement} is introduced for \( \syn\BbbS \) to mitigate this issue, the resulting theory is stronger than \logic{ZFC} with the axiom of universe.
  \end{thmenum}
\end{remark}
\begin{comments}
  \item In introductory literature, only an informal distinction between small and large sets (or between sets and classes) is often used. This includes
  \cite[def. I.3.1]{Aluffi2009Algebra},
  \cite[def. 1.1.1]{Riehl2016CategoryTheory},
  \cite[def. 1.1.1]{Perrone2024StartingCategoryTheory} and
  \cite[def. 1.1.1]{Leinster2014BasicCategoryTheory}.
\end{comments}

\paragraph{Category of small sets}

\begin{definition}\label{def:category_of_small_sets}\mcite[12; 22]{MacLane1998CategoryTheory}
  We define the \term{category of small sets} \( \cat{Set} \) as follows:
  \begin{thmenum}
    \thmitem{def:category_of_small_sets/objects} The \hyperref[def:category/objects]{objects} are, unsurprisingly, the \hyperref[def:small_set]{small sets}, i.e. the set of objects is the \hyperref[def:smallest_grothendieck_universe]{smallest Grothendieck universe} \( \mscrU_0 \).

    \thmitem{def:category_of_small_sets/morphisms} The \hyperref[def:category/morphisms]{morphisms} between small sets are the \hyperref[def:function]{total single-valued functions} between them.

    Note that, by \cref{thm:grothendieck_universe_closure/function}, functions between small sets are themselves small as sets.

    \thmitem{def:category_of_small_sets/composition} The \hyperref[def:category/composition]{composition operation} is defined for \( f: A \to B \) and \( g: B \to C \) as their \hyperref[def:set_valued_map/composition]{set-theoretic composition}.

    \thmitem{def:category_of_small_sets/identity} The \hyperref[def:category/identity]{identity morphism} on \( A \) is simply the \hyperref[def:set_valued_map/identity]{identity function} on \( A \).
  \end{thmenum}
\end{definition}
\begin{defproof}
  We must show that \( \cat{Set} \) is indeed a category.

  \SubProofOf{def:category/C1} For every pair of small sets \( A \) and \( B \) and every function \( f: A \to B \), for all \( x \) in \( A \) we have
  \begin{equation*}
    [\id_B \bincirc f](x)
    =
    \id_B(f(x))
    =
    f(x)
    =
    f(\id_A(x))
    =
    [f \bincirc \id_A](x).
  \end{equation*}

  Therefore, \( \id_A \) and \( \id_B \) satisfy \eqref{eq:def:category/C1}.

  \SubProofOf{def:category/C2} Associativity of function composition is proved in \cref{thm:def:set_valued_map/associative}.
\end{defproof}

\begin{remark}\label{rem:category_of_small_sets}
  We collect here important properties of the category \hyperref[def:category_of_small_sets]{\( \cat{Set} \)} of \hyperref[def:small_set]{small sets}. Most of them require forward references.

  \begin{thmenum}
    \thmitem{rem:category_of_small_sets/large} It is itself a \hyperref[def:small_category]{large category} because its set of objects, the \hyperref[def:smallest_grothendieck_universe]{smallest Grothendieck universe} \( \mscrU_0 \), is itself a large set (otherwise it would contain itself, contradicting the \hyperref[def:zfc/foundation]{axiom of foundation}).

    Still, \( \cat{Set} \) is \hyperref[def:small_category]{locally small}. Indeed, by \cref{thm:grothendieck_universe_closure/function}, every member of \( \cat{Set}(A, B) \) belongs to \( \mscrU_0 \), and thus, by \cref{thm:grothendieck_universe_closure/subset}, \( \cat{Set}(A, B) \) itself belongs to \( \mscrU_0 \).

    \thmitem{rem:category_of_small_sets/morphism_invertibility} All \hyperref[def:morphism_cancellation/right]{epimorphisms} and \hyperref[def:set_valued_map/empty]{nonempty} \hyperref[def:morphism_cancellation/left]{monomorphisms} \hyperref[def:morphism_invertibility/left]{split} and are precisely the \hyperref[def:function_invertibility/surjective]{surjective} and nonempty \hyperref[def:function_invertibility/injective]{injective functions}, respectively.

    This is stated in \cref{thm:function_invertibility_categorical}. See also \fullref{thm:epimorphisms_split_in_set}.

    \thmitem{rem:category_of_small_sets/universal_objects} The empty set \( \varnothing \) is an \hyperref[def:universal_objects/initial]{initial object} and any singleton set \( \set{ A } \) is a \hyperref[def:universal_objects/terminal]{terminal object}. No \hyperref[def:universal_objects/zero]{zero objects} exist in \( \cat{Set} \).

    This is discussed in \cref{ex:def:universal_objects}.

    \thmitem{rem:category_of_small_sets/discrete_category} The \hyperref[def:discrete_category]{discrete category} functor \( D: \cat{Set} \to \cat{Cat} \) is left adjoint to the forgetful functor \( U: \cat{Cat} \to \cat{Set} \)

    This is discussed in \cref{ex:def:category_adjunction/set_cat}.

    \thmitem{rem:category_of_small_sets/limits} The \hyperref[def:discrete_category_limits]{products} and \hyperref[def:discrete_category_limits]{coproducts} are the \hyperref[def:cartesian_product]{Cartesian products} and the \hyperref[def:disjoint_union]{disjoint unions}, respectively.

    This is shown in \fullref{thm:cartesian_product_universal_property} and \fullref{thm:disjoint_union_universal_property}.
  \end{thmenum}
\end{remark}

\paragraph{Category duality}

\begin{definition}\label{def:opposite_category}\mcite[33]{MacLane1998CategoryTheory}
  For each \hyperref[def:category]{category} \( \cat{C} \), we define the \term{opposite} category \( \cat{C}^\oppos \) based on the \hyperref[def:directed_multigraph/opposite]{opposite directed multigraph} as follows:
  \begin{thmenum}
    \thmitem{def:opposite_category/objects} The \hyperref[def:category/objects]{objects} coincides with those of \( \cat{C} \).

    \thmitem{def:opposite_category/morphisms} The \hyperref[def:category/morphisms]{morphisms} reverse the direction of those in \( \cat{C} \), i.e. we swap the domain and codomain functions.

    The morphism \( f^\oppos: A \to B \) from \( \cat{C}^\oppos \) is simply \( f: B \to A \) from \( \cat{C} \), and the \( \hom \)-set \( \cat{C}^\oppos(A, B) \) coincides with \( \cat{C}(B, A) \).

    \thmitem{def:opposite_category/composition} The \hyperref[def:category/composition]{composition of the morphisms}
    \begin{align*}
      f^\oppos &\in \cat{C}^\oppos(A, B) = \cat{C}(B, A) \\
      g^\oppos &\in \cat{C}^\oppos(B, C) = \cat{C}(C, B)
    \end{align*}
    is
    \begin{equation*}
      \underbrace{g^\oppos \bincirc f^\oppos}_{\cat{C}^\oppos(A, C)} \coloneqq \underbrace{f \bincirc g}_{\cat{C}(C, A)}.
    \end{equation*}

    \thmitem{def:opposite_category/identity} The \hyperref[def:category/identity]{identity morphism} on the object \( A \in \cat{C}^\oppos \) is again \( \id_A \).
  \end{thmenum}
\end{definition}
\begin{comments}
  \item Opposite categories enable \fullref{thm:category_duality}.
  \item This in in fact an \hyperref[con:opposite_object]{opposite object functor} in the \hyperref[def:category_of_small_categories]{category of small categories} \( \cat{Cat} \).
\end{comments}

\begin{example}\label{ex:def:opposite_category}
  We list some examples of \hyperref[def:opposite_category]{opposite categories}:
  \begin{thmenum}
    \thmitem{ex:def:opposite_category/sets} A morphism \( f^\oppos: A \to B \) in the category \( \cat{Set}^\oppos \) is a function from the set \( B \) to the set \( A \). We cannot, in general, regard the morphism \( f^\oppos \) as a function unless \( B \subseteq A \).
  \end{thmenum}
\end{example}

\begin{theorem}[Principle of duality for categories]\label{thm:category_duality}\mcite[\S II.1]{MacLane1998CategoryTheory}
  \hyperref[def:opposite_category]{Opposite categories} allow formulating a theory similar to \fullref{thm:order_duality}.

  Namely, consider the \hyperref[def:many_sorted_fol]{two-sorted first-order} theory of categories presented in \cref{rem:hol_theory_of_categories}. Denote by \( M^\tau_\oppos \) the transformation of the \hyperref[def:hol_expression]{logical expression} \( M^\tau \) in which we swap domains and codomains (i.e. we swap the nonlogical constants \( \synT \) and \( \synH \)), and in which we reverse the order of composition (i.e. we swap the operands of every application of \( \synmult \)).

  \begin{thmenum}
    \thmitem{thm:category_duality/assignment} For every category \( \cat{C} \), every \hyperref[def:hol_variable_assignment]{logical variable assignment} \( v \) into \( \cat{C} \) and every expression \( M^\tau \), we have
    \begin{equation*}
      \Bracks{M^\tau}_{\cat{C}}^v = \Bracks{M^\tau_\oppos}_{\cat{C}^\oppos}^v.
    \end{equation*}

    In case \( \tau \) is the \hyperref[def:quantifiable_type]{type of propositions} \( \syn\omicron \), the expression \( M \) is a \hyperref[def:hol_expression/formula]{logical formula} and the equality above is an equality of truth values.

    \thmitem{thm:category_duality/equisatisfiable} A set \( \Gamma \) of formulas is \hyperref[def:equisatisfiability]{equisatisfiable} with \( \Gamma^\oppos \).

    \thmitem{thm:category_duality/tautology} The formula \( \varphi \) is a \hyperref[def:hol_semantics/tautology]{tautology} if and only if \( \varphi^\oppos \) is.

    Informally, if a statement holds for every category, its dual statement obtained by reversing all morphisms also holds for every category.
  \end{thmenum}
\end{theorem}
\begin{comments}
  \item We list some usage examples in \cref{ex:thm:category_duality}.
  \item For more abstract concepts, it is sometimes not obvious what exactly needs to reversed. For example, \textcite[32]{MacLane1998CategoryTheory} notes the following:
  \begin{displayquote}
    We emphasize that duality for a statement involving several categories and functors between them reverses the arrows in each category but does not reverse the functors.
  \end{displayquote}

  He refers to \cref{def:opposite_functor}.
\end{comments}
\begin{proof}
  Straightforward.
\end{proof}

\begin{example}\label{ex:thm:category_duality}
  We list usage examples of \fullref{thm:category_duality}:
  \begin{thmenum}
    \thmitem{ex:thm:category_duality/morphism_invertibility} \Cref{thm:morphism_invertibility_duality}: A morphism \( f: A \to B \) in \( \cat{C} \) is a \hyperref[def:morphism_invertibility/left]{(split)} \hyperref[def:morphism_cancellation/left]{monomorphism} if and only if \( f^\oppos: B \to A \) in the opposite category \( \cat{C}^\oppos \) is a \hyperref[def:morphism_invertibility/right]{(split)} \hyperref[def:morphism_cancellation/right]{epimorphism}.

    In particular, \( f \) is an \hyperref[def:morphism_invertibility/isomorphism]{isomorphism} in \( \cat{C} \) if and only if \( f^\oppos \) is an isomorphism in \( \cat{C}^\oppos \).

    \thmitem{ex:thm:category_duality/universal_objects} \Cref{thm:universal_object_duality}: An object is \hyperref[def:universal_objects/initial]{initial} if and only if it is a \hyperref[def:universal_objects/terminal]{terminal object} the \hyperref[def:opposite_category]{opposite category}.

    \thmitem{ex:thm:category_duality/functor_categories} \Cref{thm:opposite_of_functor_category}: For the \hyperref[def:opposite_category]{opposite} of the \hyperref[def:functor_category]{functor category} \( [\cat{C}, \cat{D}] \) we have
    \begin{equation*}
      [\cat{C}, \cat{D}]^\oppos = [\cat{C}^\oppos, \cat{D}^\oppos].
    \end{equation*}

    \thmitem{ex:thm:category_duality/equivalences} \Cref{thm:opposite_of_category_equivalence}: The \hyperref[def:opposite_category]{duals} of \hyperref[def:category_equivalence]{equivalent categories} are equivalent.

    \thmitem{ex:thm:category_duality/adjunctions} \Cref{thm:category_adjunction_duality}: The functor \( F \) is \hyperref[def:category_adjunction]{left adjoint} to \( G \) if and only if the \hyperref[def:opposite_functor]{dual functor} \( F^\oppos \) is right adjoint to \( G^\oppos \).

    \thmitem{ex:thm:category_duality/limits} \Cref{thm:categorical_limit_duality}: For every \hyperref[def:category_of_cones/cone]{cone} \( (A, \alpha) \) of the \hyperref[def:categorical_diagram]{diagram} \( D \) in \( \cat{C} \), \( (A, \alpha^\oppos) \) is a \hyperref[def:category_of_cones/cone]{cocone} of \( D^\oppos \) in \( \cat{C}^\oppos \).

    Moreover, for every \hyperref[def:category_of_cones/limit]{limit} \( (L, \pi) \) of \( D \) in \( \cat{C} \), \( (L, \pi^\oppos) \) is a \hyperref[def:category_of_cones/colimit]{colimit} of \( D^\oppos \) in \( \cat{C}^\oppos \).
  \end{thmenum}
\end{example}

\paragraph{Morphism cancellation and invertibility}

\begin{definition}\label{def:morphism_cancellation}\mcite[19]{MacLane1998CategoryTheory}
  The operation cancellation properties from \cref{def:binary_operation/cancellative} generalize to \hyperref[def:category/morphisms]{categorical morphisms} as follows:
  \begin{thmenum}
    \thmitem{def:morphism_cancellation/left} We call the morphism \( g: B \to C \) \term{left cancellable} or \term{monic} if, for any pair of morphisms \( f_1, f_2: A \to B \), the equality \( g \bincirc f_1 = g \bincirc f_2 \) implies \( f_1 = f_2 \).

    Monic morphisms are also called \term[ru=мономорфизм (\cite[26]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{monomorphisms}.

    \thmitem{def:morphism_cancellation/right} Dually, we call the morphism \( f: A \to B \) \term{right cancellable} or \term[en=epic (\cite[15]{Jacobson1989BasicAlgebraII}); epi (\cite[19]{MacLane1998CategoryTheory})]{epic} if, for any pair of morphisms \( g_1, g_2: B \to C \), the equality \( g_1 \bincirc f = g_2 \bincirc f \) implies \( g_1 = g_2 \).

    Epic morphisms are also called \term[ru=эпиморфизм (\cite[29]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{epimorphisms}.

    \thmitem{def:morphism_cancellation/bimorphism}\mcite[def. 5.3.1]{Schubert1972Categories} If a morphism is both left and right cancellable, we say that it is a \term[ru=эпиморфизм (\cite[29]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{bimorphism}.
  \end{thmenum}
\end{definition}
\begin{comments}
  \item These notions are generally weaker that the corresponding invertibility notions in \cref{def:morphism_cancellation}, however they coincide in \hyperref[def:balanced_category]{balanced categories}.

  \item For the etymology of the various terms used here, see \cref{rem:morphism_etymology}.
\end{comments}

\begin{proposition}\label{thm:def:morphism_cancellation}
  Morphisms have the following basic properties regarding their cancellation properties from \cref{def:morphism_cancellation}:
  \begin{thmenum}
    \thmitem{thm:def:morphism_cancellation/composition} The composition of two monomorphisms (resp. epimorphisms) is again a monomorphism (resp. epimorphism).

    In particular, the composition of bimorphisms is a bimorphism.
  \end{thmenum}
\end{proposition}
\begin{proof}
  \SubProofOf{thm:def:morphism_cancellation/composition} Let \( g: B \to C \) and \( h: C \to D \) be monomorphisms. For arbitrary \( f_1, f_2: A \to B \), suppose that
  \begin{equation*}
    (h \bincirc g) \bincirc f_1 = (h \bincirc g) \bincirc f_2.
  \end{equation*}

  Then, by \ref{def:category/C2},
  \begin{equation*}
    h \bincirc (g \bincirc f_1) = h \bincirc (g \bincirc f_2).
  \end{equation*}

  Since \( h \) is left cancellable, it follows that
  \begin{equation*}
    g \bincirc f_1 = g \bincirc f_2.
  \end{equation*}

  Since \( g \) is also left cancellable, we have \( f_1 = f_2 \). Therefore, \( h \bincirc g \) is a monomorphism.

  The case of epimorphism follows via \fullref{thm:category_duality}.
\end{proof}

\begin{definition}\label{def:morphism_invertibility}\mcite[19]{MacLane1998CategoryTheory}
  The function invertibility notions from \cref{def:function_invertibility} generalize to \hyperref[def:category/morphisms]{categorical morphisms} as follows:
  \begin{thmenum}
    \thmitem{def:morphism_invertibility/left} We call the morphism \( f: A \to B \) \term[en=left invertible (magma element) (\cite[def. I.2.6]{Bourbaki1998Algebra1to3})]{left invertible} if there exists a morphism \( g: B \to A \) such that \( g \bincirc f = \id_A \). We call \( g \) a \term{left inverse} of \( f \).

    Left invertible morphisms are also called \term{retractions}.

    \Cref{thm:def:morphism_invertibility/split} implies that they are \hyperref[def:morphism_cancellation/left]{monomorphisms}; in fact, they are called \term[en=split monomorphism (\cite[def. 1.2.7]{Perrone2024StartingCategoryTheory})]{split monomorphisms} because they \enquote{split} the identity \( \id_A \) into a composition of \( f \) and \( g \). Using forward references to \fullref{sec:functors}, we can restate this condition by saying that the following diagram commutes:
    \begin{equation*}
      \begin{aligned}
        \includegraphics[page=1]{output/def__morphism_invertibility}
      \end{aligned}
    \end{equation*}

    \thmitem{def:morphism_invertibility/right} Dually, we call the morphism \( g: B \to A \) \term[en=right invertible (magma element) (\cite[def. I.2.6]{Bourbaki1998Algebra1to3})]{right invertible} if there exists a morphism \( f: A \to B \) such that \( f \bincirc g = \id_B \). We call \( g \) a \term{right inverse} of \( f \).

    Right invertible morphisms are also called \term{sections}.

    \Cref{thm:def:morphism_invertibility/split} implies that they are \hyperref[def:morphism_cancellation/left]{monomorphisms}; in fact, they are also called \term[en=split monomorphism (\cite[def. 1.2.7]{Perrone2024StartingCategoryTheory})]{split epimorphisms} because they \enquote{split} the identity \( \id_A \) into a composition of \( f \) and \( g \). Using forward references to \fullref{sec:functors}, we can restate this condition by saying that the following diagram commutes:
    \begin{equation*}
      \begin{aligned}
        \includegraphics[page=2]{output/def__morphism_invertibility}
      \end{aligned}
    \end{equation*}

    \thmitem{def:morphism_invertibility/isomorphism} If the morphism \( f: A \to B \) is both left and right invertible, \cref{thm:def:morphism_invertibility/left_and_right} implies that all left and right inverses coincide. We denote this unique morphism by \( f^{-1}: B \to A \) and call it the \term[en=two-sided inverse (of functor) (\cite[14]{MacLane1998CategoryTheory})]{two-sided inverse} or simply the \term{inverse} of \( f \). Naturally, in this case we say that \( f \) is \term{invertible}.

    Invertible morphisms are also called \term[ru=изоморфизмы (\cite[30]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{isomorphisms}. \Cref{thm:def:morphism_invertibility/isomorphism_equivalence} implies that the existence of an isomorphism between objects is an \hyperref[def:equivalence_relation]{equivalence relation}. Under this relation, the isomorphism itself is irrelevant; we say that \( A \) and \( B \) are \term[ru=изоморфные (объекты) (\cite[31]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{isomorphic} and write \( A \cong B \).
  \end{thmenum}
\end{definition}
\begin{comments}
  \item These notions are generally stronger that the corresponding cancellation notions in \cref{def:morphism_invertibility}, however they coincide in \hyperref[def:balanced_category]{balanced categories}.

  \item For the etymology of the various terms used here, see \cref{rem:morphism_etymology}.
\end{comments}

\begin{example}\label{ex:def:morphism_invertibility}
  We list examples of the morphism invertibility notions from \cref{def:morphism_invertibility}:
  \begin{thmenum}
    \thmitem{ex:def:morphism_invertibility/set} \Cref{thm:function_invertibility_categorical} characterizes morphism cancellation and invertibility for \hyperref[def:category_of_small_sets]{\( \cat{Set} \)} in terms of \hyperref[def:function_invertibility/injective]{injectivity} and \hyperref[def:function_invertibility/surjective]{surjectivity}.

    \thmitem{ex:def:morphism_invertibility/not_monomorphism} A very simple example of a monomorphism which does not split is an empty function with a nonempty domain.

    \thmitem{ex:def:morphism_invertibility/epimorphisms_split} \Fullref{thm:surjective_functions_are_right_invertible} is important enough to have a categorical interpretation via \fullref{thm:epimorphisms_split_in_set}, where its relation to the \hyperref[def:zfc/choice]{axiom of choice} is also discussed.
  \end{thmenum}
\end{example}

\begin{proposition}\label{thm:morphism_invertibility_duality}
  A morphism \( f: A \to B \) in \( \cat{C} \) is a \hyperref[def:morphism_invertibility/left]{(split)} \hyperref[def:morphism_cancellation/left]{monomorphism} if and only if \( f^\oppos: B \to A \) in the opposite category \( \cat{C}^\oppos \) is a \hyperref[def:morphism_invertibility/right]{(split)} \hyperref[def:morphism_cancellation/right]{epimorphism}.

  In particular, \( f \) is a bimorphism (resp. isomorphism) in \( \cat{C} \) if and only if \( f^\oppos \) is a bimorphism (resp. isomorphism) in \( \cat{C}^\oppos \).
\end{proposition}
\begin{proof}
  Straightforward in light of \fullref{thm:category_duality}.
\end{proof}

\begin{definition}\label{def:balanced_category}\mcite[def. 5.3.3]{Schubert1972Categories}
  We say that a \hyperref[def:category]{category} is \term{balanced} if all its \hyperref[def:morphism_cancellation/bimorphism]{bimorphisms} are \hyperref[def:morphism_invertibility/isomorphism]{invertible}.
\end{definition}

\begin{example}\label{ex:def:balanced_category}
  We list examples of (un)\hyperref[def:balanced_category]{balanced} categories:
  \begin{thmenum}
    \thmitem{ex:def:balanced_category/set} \Cref{thm:category_of_sets_is_balanced} implies that the \hyperref[def:category_of_small_sets]{category of small sets} \( \cat{Set} \) is balanced.

    \thmitem{ex:def:balanced_category/equational} For the \hyperref[def:fol_theory_model_functor/obj]{category of small models} of an \hyperref[def:fol_equational_theory]{equational first-order theory}, \cref{thm:concrete_category_function_invertibility/injective} implies that every monomorphism splits.

    Epimorphisms in may not split, however. For example, in the category of small monoids, the embedding \( \iota: \BbbN \to \BbbZ \) is an epimorphism due to \fullref{thm:group_of_differences_universal_property}, but it is not surjective.

    Still, equational theories are nice enough for a sufficient condition like \cref{thm:fol_equational_theory_balanced_category} --- the category of (small) models is balanced as long as epimorphisms are surjective.

    \thmitem{ex:def:balanced_category/top} The category \hyperref[def:category_of_small_topological_spaces]{\( \cat{Top} \)} has both left and right adjoints. \Cref{thm:concrete_category_function_invertibility} implies that every monomorphism is injective and every epimorphism is surjective.

    This is not an equational theory, however, so even bijective homeomorphisms may fail to have a continuous inverse. So the category is not balanced.
  \end{thmenum}
\end{example}

\begin{definition}\label{def:categorical_endomorphism}\mcite[def. 1.3.1]{Schubert1972Categories}
  We say that a \hyperref[def:category/morphisms]{categorical morphism} from an object to itself is an \term[ru=эндоморфизм (\cite[42]{Хелемский2014ФункциональныйАнализ})]{endomorphism}. If it is \hyperref[def:morphism_invertibility/isomorphism]{invertible}, say that it is an \term[ru=автоморфизм (\cite[50]{Хелемский2014ФункциональныйАнализ})]{automorphism}.
\end{definition}

\begin{remark}\label{rem:morphism_etymology}
  Many terms in mathematics grew out of the Greek noun \enquote{\textel{μορφή}} (transliterated as \enquote{morphi} and translated by \textcite[948]{DiggleEtAl2021CambridgeGreekLexicon} as \enquote{kind, sort or aspect} when applied to \enquote{non-physical or abstract things}). We list some of these words in \cref{tab:rem:morphism_etymology}; most have classical Greek prefixes, with spelling and translations taken from \cite{DiggleEtAl2021CambridgeGreekLexicon}, while the rest are derived in an obvious way.

  \begin{table}
    \centering
    \begin{tabular}{l l l l}
      \toprule
      Term                                                           & Greek prefix    & Prefix transliteration & Prefix translation \\
      \midrule
      \hyperref[con:logical_model]{Homomorphism}                     & \textel{όμός}   & homos                  & same, common       \\
      \hyperref[def:morphism_cancellation/left]{Monomorphism}        & \textel{μόνος}  & monos                  & alone, separated   \\
      \hyperref[def:morphism_cancellation/right]{Epimorphism}        & \textel{ἐπί}    & epi                    & on, upon, over     \\
      \hyperref[def:morphism_cancellation/bimorphism]{Bimorphism}    & ---             & ---                    & ---                \\
      \hyperref[def:morphism_invertibility/isomorphism]{Isomorphism} & \textel{ἴσος}   & isos                   & equal, similar     \\
      \hyperref[def:categorical_endomorphism]{Endomorphism}          & \textel{ἔνδον}  & endon                  & inside, within     \\
      \hyperref[def:categorical_endomorphism]{Automorphism}          & \textel{αὑτός}  & autos                  & alone, on its own  \\
      \hyperref[def:homeomorphism]{Homeomorphism}                    & \textel{ὅμοιος} & homoios                & alike, similar     \\
      \hyperref[def:diffeomorphism]{Diffeomorphism}                  & ---             & ---                    & ---                \\
      \hyperref[def:holomorphic_function]{Holomorphic function}      & \textel{ὅλος}   & holos                  & while, entire      \\
      \hyperref[def:biholomorphic_function]{Biholomorphic function}  & ---             & ---                    & ---                \\
      \hyperref[def:holomorphic_function]{Meromorphic function}      & \textel{μέρος}  & meros                  & portion, share     \\
      \bottomrule
    \end{tabular}
    \caption{Common terms based on the word \enquote{morphism}.}\label{tab:rem:morphism_etymology}
  \end{table}
\end{remark}

\begin{definition}\label{def:categorical_involution}\mimprovised
  We say that a \hyperref[def:categorical_endomorphism]{automorphism} is an \term[ru=инволюция (множеств) (\cite[65]{Гуров2013ТеорияРешёток})]{involution} if it is its own inverse.
\end{definition}
\begin{comments}
  \item An involution is a member of the \hyperref[def:automorphism_group]{automorphism group} of \hyperref[def:group_element_order]{order} \( 2 \).

  In the category of sets, \textcite[65]{Гуров2013ТеорияРешёток} calls any members of \( \fun(A) \) of order \( 2 \) an \enquote{\textru{инволюция}} (\enquote{involution}) of \( A \).

  \Textcite[97]{Розенфельд1963ОсновныеПонятиеГеометрии} use the term loosely for any transformations in a geometric context. On the other hand, \textcite[292]{Kline1972MathematicalThoughtVol1} and \textcite[119]{Schwartzman1996WordsOfMathematics} suggest historical geometric constructions using the same name.
\end{comments}

\begin{proposition}\label{thm:def:morphism_invertibility}
  Morphisms have the following basic properties regarding their invertibility properties from \cref{def:morphism_invertibility}:
  \begin{thmenum}
    \thmitem{thm:def:morphism_invertibility/split} Any left (resp. right) \hyperref[def:morphism_invertibility]{invertible morphism} is left (resp. right) \hyperref[def:morphism_cancellation]{cancellable}.

    \thmitem{thm:def:morphism_invertibility/left_and_right} If a morphism is both left invertible and right invertible, the two inverses are equal, and the morphism is fully invertible.

    \thmitem{thm:def:morphism_invertibility/unique_inverse} Any morphism has at most one two-sided inverse.

    \thmitem{thm:def:morphism_invertibility/inverse_duality} The morphism \( f: A \to B \) is a right inverse of \( g: B \to A \) if and only if \( g \) is a left inverse of \( f \).

    In particular, the inverse of an isomorphism is an isomorphism.

    \thmitem{thm:def:morphism_invertibility/isomorphism_equivalence} Isomorphism in \( \cat{C} \) is an \hyperref[def:equivalence_relation]{equivalence relation} on the objects of \( \cat{C} \).

    \thmitem{thm:def:morphism_invertibility/cancellable_and_invertible} If a morphism left cancellable and right invertible, it is an isomorphism.

    Dually, if a morphism left invertible and right cancellable, it is again an isomorphism.

    \thmitem{thm:def:morphism_invertibility/composition} The composition of two left (resp. right) invertible morphisms is again left (resp. right) invertible.

    In particular, the composition of isomorphisms is a isomorphism.
  \end{thmenum}
\end{proposition}
\begin{proof}
  \SubProofOf{thm:def:morphism_invertibility/split} Suppose that \( g: B \to C \) is left invertible with inverse \( h: C \to B \). Suppose that \( f_1, f_2: A \to B \) are morphisms such that
  \begin{equation*}
    g \bincirc f_1 = g \bincirc f_2.
  \end{equation*}

  Then
  \begin{equation*}
    f_1
    \reloset {\eqref{eq:def:category/C1}} =
    \id_B \bincirc f_1
    =
    (h \bincirc g) \bincirc f_1
    \reloset {\eqref{eq:def:category/C2}} =
    h \bincirc (g \bincirc f_1)
    =
    h \bincirc (g \bincirc f_2)
    =
    \cdots
    =
    f_2.
  \end{equation*}

  If \( g: B \to C \) is right invertible, it follows by duality that it has a right inverse.

  \SubProofOf{thm:def:morphism_invertibility/left_and_right} Suppose that \( f: A \to B \) has a left inverse \( l: B \to A \) and a right inverse \( r: B \to A \). Then
  \begin{equation*}
    l
    \reloset {\eqref{eq:def:category/C1}} =
    l \bincirc \id_A
    =
    l \bincirc (f \bincirc r)
    \reloset {\eqref{eq:def:category/C2}} =
    (l \bincirc f) \bincirc r
    =
    \id_B \bincirc r
    \reloset {\eqref{eq:def:category/C1}} =
    r.
  \end{equation*}

  \SubProofOf{thm:def:morphism_invertibility/unique_inverse} Follows from \cref{thm:def:morphism_invertibility/left_and_right}.

  \SubProofOf{thm:def:morphism_invertibility/inverse_duality} Follows form \fullref{thm:category_duality}.

  \SubProofOf{thm:def:morphism_invertibility/cancellable_and_invertible} Suppose that \( g: B \to A \) is left cancellable and right invertible. Let \( f: A \to B \) be a right inverse of \( g \). Then
  \begin{equation*}
    f
    =
    \reloset {\eqref{eq:def:category/C1}} =
    f \bincirc \id_A
    =
    f \bincirc (g \bincirc f)
    =
    (f \bincirc g) \bincirc f.
  \end{equation*}

  Since \( g \) is a left inverse of \( f \), from \cref{thm:def:morphism_invertibility/split} it follows that \( f \) is left cancellable. Since we have
  \begin{equation*}
    \id_B \bincirc f
    =
    (f \bincirc g) \bincirc f,
  \end{equation*}
  it follows that \( f \bincirc g = \id_B \).

  Therefore, \( f \) is a left inverse of \( g \). By \cref{thm:def:morphism_invertibility/left_and_right}, it is a two-sided inverse.

  The case where \( g \) is left invertible and right cancellable follows by duality.

  \SubProofOf{thm:def:morphism_cancellation/composition} Let \( g: B \to C \) and \( h: C \to D \) be monomorphisms.

  Let \( f_1, f_2: A \to B \) be two arbitrary morphisms with codomain \( B \). Suppose that
  \begin{equation*}
    (h \bincirc g) \bincirc f_1 = (h \bincirc g) \bincirc f_2.
  \end{equation*}

  Then, by associativity,
  \begin{equation*}
    h \bincirc (g \bincirc f_1) = h \bincirc (g \bincirc f_2).
  \end{equation*}

  Since \( h \) is left cancellable, it follows that
  \begin{equation*}
    g \bincirc f_1 = g \bincirc f_2.
  \end{equation*}

  Since \( g \) is also left cancellable, we have \( f_1 = f_2 \). Therefore, \( h \bincirc g \) is a monomorphism.

  By duality, if \( g \) and \( h \) are epimorphisms, so is their composition.
\end{proof}

\begin{theorem}[Epimorphisms split in Set]\label{thm:epimorphisms_split_in_set}
  Every \hyperref[def:morphism_cancellation/right]{epimorphism} in the \hyperref[def:category_of_small_sets]{category of small sets} \hyperref[def:morphism_invertibility/left]{splits}.
\end{theorem}
\begin{comments}
  \item In \hyperref[def:zfcu]{\logic{ZF+U}}, this theorem is equivalent to the \hyperref[def:zfc/choice]{axiom of choice}. We list equivalent results in \cref{thm:axiom_of_choice_equivalences}.

  This theorem can be regarded as a categorical statement of the axiom of choice.
\end{comments}
\begin{proof}
  By \cref{thm:function_invertibility_categorical/right_cancellable}, a function is an epimorphism if and only if it is surjective. Thus, the theorem is equivalent to \fullref{thm:surjective_functions_are_right_invertible}.
\end{proof}

\paragraph{Universal objects}

\begin{definition}\label{def:universal_objects}\mcite[20]{MacLane1998CategoryTheory}
  Fix a category \( \cat{C} \).

  \begin{thmenum}
    \thmitem{def:universal_objects/initial} We call the object \( I \in \cat{C} \) an \term[ru=начальный объект (\cite[80]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{initial object} if for any other object \( A \in \cat{C} \) there exists a unique morphism \( f: I \to A \),

    \thmitem{def:universal_objects/terminal} Dually, we call the object \( T \in \cat{C} \) a \term[ru=конечный объект (\cite[80]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{terminal object} or \term{final object} if for any other object \( A \in \cat{C} \) there exists a unique morphism \( f: A \to T \).

    The initial and terminal objects are collectively called \term{universal objects}.

    \thmitem{def:universal_objects/zero} If \( Z \) is both an initial and a terminal object, we say that \( Z \) is a \term[ru=нулевой объект (\cite[80]{ЦаленкоШульгейфер1974ОсновыТеорииКатегорий})]{null object} or \term{zero object}.
  \end{thmenum}
\end{definition}

\begin{example}\label{ex:def:universal_objects}
  \begin{thmenum}
    \thmitem{ex:def:universal_objects/set} In the category \hyperref[def:category_of_small_sets]{\( \cat{Set} \)} of small sets, for any set \( A \) there is a unique \hyperref[def:set_valued_map/empty]{empty function} from \( \varnothing \) to \( A \). Therefore, \( \varnothing \) is an \hyperref[def:universal_objects/initial]{initial object} in \( \cat{Set} \).

    For any set \( A \), there is a unique function that contracts any set \( B \) to \( \set{ A } \). Therefore, every singleton set is a \hyperref[def:universal_objects/terminal]{final object} in \( \cat{Set} \).

    We often denote the initial and terminal objects in \( \cat{Set} \) by \( 0 \) and \( 1 \) respectively, which corresponds to their definition as \hyperref[def:ordinal]{ordinals}.

    By \cref{thm:def:universal_objects/no_zero}, \( \cat{Set} \) has no zero object.

    \thmitem{ex:def:universal_objects/grp} In the category \hyperref[def:group/category]{\( \cat{Grp} \)} of small groups, the \hyperref[def:group/trivial]{trivial group} is a \hyperref[def:universal_objects/zero]{zero object}. This holds more generally for pointed sets rather than groups.

    Indeed, it can be embedded into any other group and any group can be contracted into the corresponding trivial group. Furthermore, all trivial groups are isomorphic.
  \end{thmenum}
\end{example}

\begin{proposition}\label{thm:universal_object_duality}
  An object is \hyperref[def:universal_objects/initial]{initial} if and only if it is a \hyperref[def:universal_objects/terminal]{terminal object} the opposite category.

  This is part of the duality principles listed in \fullref{thm:category_duality}.
\end{proposition}
\begin{proof}
  Trivial.
\end{proof}

\begin{proposition}\label{thm:def:universal_objects}
  \hfill
  \begin{thmenum}
    \thmitem{thm:def:universal_objects/initial} An \hyperref[def:universal_objects/initial]{initial object} is unique up to an isomorphism.
    \thmitem{thm:def:universal_objects/terminal} Dually, a \hyperref[def:universal_objects/initial]{terminal object} is also unique up to an isomorphism.
    \thmitem{thm:def:universal_objects/zero} If a category has an initial and a terminal object and if they are isomorphic, then both are zero objects.

    In particular, a zero object is unique up to an isomorphism.

    \thmitem{thm:def:universal_objects/no_zero} If an initial and a terminal object exists and are not isomorphic, then there exist no zero objects.
  \end{thmenum}
\end{proposition}
\begin{proof}
  \SubProofOf{thm:def:universal_objects/initial} Suppose that \( A \) and \( B \) are both initial objects in \( \cat{C} \). Then there exist morphisms \( f: A \to B \) and \( g: B \to A \). Their composition \( g \bincirc f \) is an \hyperref[def:categorical_endomorphism]{endomorphism} on \( A \).

  But there exists a unique \hyperref[def:categorical_endomorphism]{endomorphism} on \( A \), which must be the identity \( \id_A \). Thus, \( g \bincirc f = \id_A \) and \( g \) is a left inverse of \( f \).

  We can analogously show that \( g \) is a right inverse of \( f \). Therefore, \( f \) is fully invertible, and \( A \) and \( B \) are isomorphic.

  \SubProofOf{thm:def:universal_objects/terminal} If \( T' \) and \( T^\dprime \) are terminal objects in \( \cat{C} \), by \cref{thm:universal_object_duality}, they are initial objects in \( \cat{C}^\oppos \). By \cref{thm:def:universal_objects/initial}, they are isomorphic in \( \cat{C}^\oppos \) and by \cref{thm:morphism_invertibility_duality}, they are isomorphic in \( \cat{C} \).

  \SubProofOf{thm:def:universal_objects/zero} Suppose that \( A \) is an initial object and that \( B \) is a final object in \( \cat{C} \). Let \( f: A \to B \) be an isomorphism between them.

  Let \( C \in \cat{C} \) be any other object and let \( g: C \to B \) be the unique morphism to \( B \). Then \( f^{-1} \bincirc g: C \to A \) is a morphism from \( C \) to \( A \). The inverse \( f^{-1}: B \to A \) is unique by \cref{thm:def:morphism_invertibility/unique_inverse}, therefore its composition with \( g: C \to B \) is also unique. Hence, any object has a unique morphism to \( A \). This makes \( A \) a terminal object and thus a zero object.

  We can analogously show that \( B \) is a zero object.

  \SubProofOf{thm:def:universal_objects/no_zero} By \cref{thm:def:universal_objects/zero}, all zero objects are isomorphic. By \cref{thm:def:universal_objects/initial}, all initial objects are isomorphic and analogously for terminal objects. Hence, if a zero object exists, all initial objects are isomorphic to all terminal objects.

  If some initial object is not isomorphic to some terminal object, then by contraposition it follows that no zero object exists.
\end{proof}
