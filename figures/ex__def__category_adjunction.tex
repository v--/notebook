\documentclass{classes/tikzcd}

\begin{document}
  \begin{tikzcd}[column sep=huge]
    \cat{Cat}(D(A), \cat{C}) \ar[r, "{\cat{Cat}(D(f), F)}"] \ar[d, "U" swap] & \cat{Cat}(D(B), \cat{D}) \ar[d, "U"] \\
    \cat{Set}(A, U(\cat{C})) \ar[r, "{\cat{Set}(f, U(F))}" swap]             & \cat{Set}(B, U(\cat{D}))
  \end{tikzcd}

  \begin{tikzcd}[column sep=tiny]
    D(A) \ar[rr, "D(\id_A)"] \ar[rd, "D(\id_A)" swap] &                                                             & D(A) \\
                                                      & {[D \bincirc U \bincirc D](A)} \ar[ur, "\Iota_{D(A)}" swap] &
  \end{tikzcd}

  \begin{tikzcd}[column sep=tiny]
    U(\cat{C}) \ar[rr, "U(\id_{\cat{C}})"] \ar[rd, "\id_{U(\cat{C})}" swap] &                                                                         & U(\cat{C}) \\
                                                                            & {[U \bincirc D \bincirc U](\cat{C})} \ar[ur, "U(\Iota_{\cat{C}})" swap] &
  \end{tikzcd}

  \begin{tikzcd}[column sep=huge]
    \cat{Cat}(F(Q), \cat{C}) \ar[r, "{\cat{Cat}(F(g_V, g_E), G)}"] \ar[d, "\varphi_{Q, \cat{C}}" swap] & \cat{Cat}(F(R), \cat{D}) \ar[d, "\varphi_{R, \cat{D}}"] \\
    \cat{Set}(Q, U(\cat{C})) \ar[r, "{\cat{Set}((g_V, g_E), U(G))}" swap]                              & \cat{Set}(R, U(\cat{D}))
  \end{tikzcd}

  \begin{tikzcd}[column sep=tiny]
    F(Q) \ar[rr, "F(\id_Q)"] \ar[rd, "F(\iota_Q)" swap] &                                                                   & F(Q) \\
                                                        & {[F \bincirc U \bincirc F](Q)} \ar[ur, "\varepsilon_{F(Q)}" swap] &
  \end{tikzcd}

  \begin{tikzcd}[column sep=tiny]
    U(\cat{C}) \ar[rr, "U(\iota_{\cat{C}})"] \ar[rd, "\iota_{U(\cat{C})}" swap] &                                                                               & U(\cat{C}) \\
                                                                                & {[U \bincirc F \bincirc U](\cat{C})} \ar[ur, "U(\varepsilon_{\cat{C}})" swap] &
  \end{tikzcd}

  \begin{tikzcd}
    [
      row sep=tiny,
      column sep=small,
      execute at end picture=
       {
         \node[LightFill, LightDraw, fit=(tikz@f@7-1-2)(tikz@f@7-3-2)]{};
         \node[LightFill, LightDraw, fit=(tikz@f@7-1-6)(tikz@f@7-3-6)]{};
       }
    ]
           & \Anon \ar[rrrr, dashed]         &&&& \Anon         &   \\
    D_S(G) &                                 &&&&               & Q \\
           & \Anon \ar[rrrr, dashed] \ar[uu] &&&& \Anon \ar[uu] &
  \end{tikzcd}

  \begin{tikzcd}
    [
      row sep=tiny,
      column sep=small,
      execute at end picture=
       {
         \node[LightFill, LightDraw, fit=(tikz@f@8-1-2)(tikz@f@8-3-2)]{};
         \node[LightFill, LightDraw, fit=(tikz@f@8-1-6)(tikz@f@8-3-6)]{};
       }
    ]
           & \Anon \ar[ddrrrr, dashed]                        &&&& \Anon         &   \\
    D_S(G) &                                                  &&&&               & Q \\
           & \Anon \ar[uurrrr, dashed, crossing over] \ar[uu] &&&& \Anon \ar[uu] &
  \end{tikzcd}
\end{document}
