\documentclass{classes/tikzcd}

\begin{document}
  \begin{tikzcd}[column sep=huge]
    \cat{Set}(A, U(X)) \ar[r, "{\cat{Set}(\id_A, U(f))}"] \ar[d, "\varphi_{A, X}" swap] & \cat{Set}(A, U(Y)) \ar[d, "\varphi_{A, Y}"] \\
    \cat{C}(F(A), X) \ar[r, "{\cat{C}(\id_{F(A)}, f)}" swap]                            & \cat{C}(F(A), Y)
  \end{tikzcd}
\end{document}
