\documentclass{../tikzcd}

\begin{document}
  \begin{tikzcd}
    \overbrace{F_X(A)}^{X} \ar[rr, "F_X(f) = \id_X"] \ar[d, swap, "\alpha_A"] && \overbrace{F_X(B)}^{X} \ar[d, "\alpha_B"] \\
    \underbrace{F_Y(A)}_{Y} \ar[rr, swap, "F_Y(f) = \id_Y"]                   && \underbrace{F_Y(B)}_{Y}
  \end{tikzcd}
\end{document}
