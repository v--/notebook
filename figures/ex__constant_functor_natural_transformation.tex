\documentclass{../tikzcd}

\begin{document}
  \begin{tikzcd}
    \overbrace{F_X(A)}^{A} \ar[rr, "F_X(f) = \id_A"] \ar[d, swap, "\alpha_A"] && \overbrace{F_X(B)}^{A} \ar[d, "\alpha_B"] \\
    \underbrace{F_Y(A)}_{B} \ar[rr, swap, "F_Y(f) = \id_B"]                   && \underbrace{F_Y(B)}_{B}
  \end{tikzcd}
\end{document}
