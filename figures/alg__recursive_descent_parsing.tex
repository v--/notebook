\documentclass{classes/forest}

\begin{document}
  \begin{forest}
    [
      \( A \), for tree={l=0}
        [\( \varepsilon \)]
    ]
  \end{forest}

  \begin{forest}
    [
      \( A \)
        [\( u_0 \)]
        [\( B_1 \)]
        [\( u_1 \)]
        [\( B_2 \)]
        [\( \cdots \), no edge]
        [\( u_{r-1} \)]
        [\( B_r \)]
        [\( u_r \)]
    ]
  \end{forest}

  \begin{forest}
    [
        [
          prefix, no edge
          [\( a_1 \)]
          [\( \cdots \), no edge]
          [\( a_{k_{j-1}} \)]
        ]
        [
          \( w_j \), no edge
          [\( a_{k_{j-1} + 1} \)]
          [\( \cdots \), no edge]
          [\( a_{k_j - \len(u_j)} \)]
        ]
        [
          \( u_j \), no edge
          [\( a_{k_j - \len(u_j) + 1} \)]
          [\( \cdots \), no edge]
          [\( a_{k_j} \)]
        ]
        [
          suffix, no edge
          [\( a_{k_j + 1} \)]
          [\( \cdots \), no edge]
          [\( a_n \)]
        ]
    ]
  \end{forest}

  \begin{forest}
    [
      \( A \)
        [\( b_0^{(0)} \)]
        [\( \cdots \), no edge]
        [\( b_{l_0}^{(0)} \)]
        [\( T_1 \), SubTree]
        [\( b_0^{(1)} \)]
        [\( \cdots \), no edge]
        [\( b_{l_{s-1}}^{(s-1)} \)]
        [\( T_s \), SubTree]
        [\( b_0^{(s)} \)]
        [\( \cdots \), no edge]
        [\( b_{l_s}^{(s)} \)]
    ]
  \end{forest}
\end{document}
