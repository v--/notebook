\documentclass{../tikzcd}

\begin{document}
  \begin{tikzcd}
    \Bullet \ar[r, shift left] \ar[r, shift right] & \Bullet
  \end{tikzcd}

  \begin{tikzcd}
    V \ar[r, shift left, "h"] \ar[r, shift right, "t" {swap}] & A
  \end{tikzcd}

  \begin{tikzcd}
    Q(A) \ar[r, "Q(h)"] \ar[d, "f_A" {swap}] & Q(V) \ar[d, "f_V"] \\
    R(A) \ar[r, swap, "R(h)"]               & R(V)
  \end{tikzcd}

  \begin{tikzcd}
    Q(A) \ar[r, "Q(t)"] \ar[d, "f_A" {swap}] & Q(V) \ar[d, "f_V"] \\
    R(A) \ar[r, swap, "R(t)"]               & R(V)
  \end{tikzcd}
\end{document}
