\documentclass{classes/tikzcd}

\begin{document}
  \begin{tikzcd}
    \Anon \ar[r, shift left] \ar[r, shift right] & \Anon
  \end{tikzcd}

  \begin{tikzcd}
    V \ar[r, shift left, "h"] \ar[r, shift right, "t" swap] & A
  \end{tikzcd}

  \begin{tikzcd}
    Q(A) \ar[r, "Q(h)"] \ar[d, "f_A" swap] & Q(V) \ar[d, "f_V"] \\
    R(A) \ar[r, "R(h)" swap]               & R(V)
  \end{tikzcd}

  \begin{tikzcd}
    Q(A) \ar[r, "Q(t)"] \ar[d, "f_A" swap] & Q(V) \ar[d, "f_V"] \\
    R(A) \ar[r, "R(t)" swap]               & R(V)
  \end{tikzcd}
\end{document}
