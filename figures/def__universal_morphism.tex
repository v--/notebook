\documentclass{classes/tikzcd}

\begin{document}
  \begin{tikzcd}[column sep=large, row sep=large]
    A \ar[d, "\iota" swap] \ar[r, "f"]                          & U(X) \\
    {U(\widetilde{A})} \ar[ur, dashed, "U(\widetilde{f})" swap] &
  \end{tikzcd}

  \begin{tikzcd}[column sep=large, row sep=large]
    U(X) \ar[r, "f"] \ar[dr, dashed, "U(\widetilde{f})" swap] & A                                     \\
                                                              & {U(\widetilde{A})} \ar[u, "\pi" swap]
  \end{tikzcd}
\end{document}
